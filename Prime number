#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;int c=0;
    cin>>n;
    for(int i=2;i<=sqrt(n);i++)
    {
        if(n%i==0)
        {c++; break;}
    }
    if(c==1)
    cout<<"Not Prime Number";
    else
    cout<<"Prime Number";
    return 0;
}
